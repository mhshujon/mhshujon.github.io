import {EMAIL_URL, GITHUB_URL, LINKEDIN_URL, WHATSAPP_URL} from "./socialLinks.ts";

export const contacts = {
    github: GITHUB_URL,
    linkedin: LINKEDIN_URL,
    whatsapp: WHATSAPP_URL,
    email: EMAIL_URL
}